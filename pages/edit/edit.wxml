<!--pages/edit/edit.wxml-->
  <form bindsubmit="formSubmit" bindreset="formReset">
  <view class="section">
    <input name="title" name="text" value="{{wills.text}}"  class="input-title"/>
    <textarea name="content" maxlength="-1" value="{{wills.content}}" class="input-content"></textarea>
  </view>
  <view class="btn-area">
    <button formType="submit">保存</button>
    <button formType="reset">重写</button>
  </view>
 </form>
